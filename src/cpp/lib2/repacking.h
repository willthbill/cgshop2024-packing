#pragma once

#include <bits/stdc++.h>

#include "io.h"

class HeuristicRepacking {

private:

public:

    PackingOutput run(PackingInput _input, PackingOutput _initial); 

    PackingOutput _run(PackingInput input, PackingOutput initial);

};
